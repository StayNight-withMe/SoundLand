<Window x:Class="WpfApp1.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        WindowState="Normal"
        WindowStyle="None"
        mc:Ignorable="d"
        Title="Window1" Height="900" Width="1920">



    <Window.Resources>



        <Style x:Key="CustomSliderStyle" TargetType="Slider">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Black" />
            <Setter Property="Foreground" Value="Black" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Grid>
                            <Track x:Name="PART_Track" Grid.Row="1">
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Ellipse Width="20" Height="20" Fill="Black" />
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                            <Border x:Name="PART_TrackBackground"
                                Background="Transparent"
                                BorderBrush="Black"
                                BorderThickness="1"
                                Height="4"
                                VerticalAlignment="Center"
                                CornerRadius="2"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>


        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Black" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                       BorderBrush="{TemplateBinding BorderBrush}"
                       BorderThickness="1"
                       CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RoundedTextBoxStyle" TargetType="TextBox">
            <Setter Property="BorderThickness" Value="3" />
            <Setter Property="BorderBrush" Value="Black" />
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="10">
                            <ScrollViewer x:Name="PART_ContentHost" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

     


    </Window.Resources>








    <Grid x:Name="GridOfWindow" MouseLeftButtonDown="GridOfWindow_MouseLeftButtonDown">

        

        <Grid>
            <MediaElement x:Name="BackGroundPlayer" LoadedBehavior="Manual" Stretch="UniformToFill" MediaEnded="BackGroundPlayer_MediaEnded" />

        </Grid>

        <Grid>

            <Grid.RowDefinitions>
                <RowDefinition Height="11*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="3*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>



            <Button x:Name="close" Grid.Column="2" Width="25" Height="25" Background="Black" HorizontalAlignment="Right" VerticalAlignment="Top" Click="close_Click"/>
            <Button x:Name="Minimized" Grid.Column="2" Width="25" Height="25" Background="Black" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0 0 30 0" Click="Minimized_Click"/>


            <TabControl  x:Name="tab" Grid.Column="1" Grid.Row="0" Background="Transparent">

                <TabItem Header="Поиск"  Background="Gray" >


                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="19*"/>
                            <ColumnDefinition Width="173*"/>
                        </Grid.ColumnDefinitions>



                        <Grid.RowDefinitions>

                            <RowDefinition Height = "0.7*" />
                            <RowDefinition Height = "0.5*" />
                            <RowDefinition Height = "7*" />
                            <RowDefinition Height = "*" />


                        </Grid.RowDefinitions>



                        <Grid Grid.Row="3" Grid.ColumnSpan="2" >

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="0.7*"/>
                                <ColumnDefinition Width="6*"/>
                                <ColumnDefinition Width="0.5*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>


                            <Button x:Name="PlayPauseBtn" Content="Pouse" Style="{StaticResource ButtonStyle}"
        Height="50" Width="50" Grid.Column="1" 
        Margin="0 0 0 0"  HorizontalAlignment="Left" Click="PlayPauseBtn_Click" RenderTransformOrigin="0.5,0.5"/>


                            <MediaElement x:Name="MediaPlayer"  Visibility="Collapsed" 
              LoadedBehavior="Manual" Volume="{Binding ElementName=slider, Path=Value}"  UnloadedBehavior="Stop"/>


                            <Image x:Name="PlaySong" 
    Width="70"
       Grid.Column="0"
    HorizontalAlignment="Left"
   Margin="30 0 0 0"
    Height="60" />


                            <ProgressBar x:Name="ProgressBar" 
       Background="Black" 
       Foreground="White" 
       HorizontalAlignment="Stretch" 
       VerticalAlignment="Center"
       Margin="10 0 20 10"
       Minimum="0"
       Value="0" 
       Grid.Column="2"
       Height="10"
       MouseDown="ProgressBar_MouseDown"
       MouseMove="ProgressBar_MouseMove"
       MouseUp="ProgressBar_MouseUp"
       IsEnabled="True"/>

                            <TextBlock x:Name="Duration" Text="00:00" Foreground="Black" Grid.Column="3" HorizontalAlignment="Right" Margin="0 30 0 0" FontSize="16"/>

                            <Slider x:Name="slider"
       Style="{StaticResource CustomSliderStyle}"
    Height="30"
    Width="100"
  HorizontalAlignment="Right"
   Margin="0 0 10 0"
        Minimum="0" Maximum="1" Value="{Binding ElementName=MediaPlayer,Path=Volume}"
    Background="Transparent" Foreground="Black" BorderBrush="Black" 
                  Grid.Column="4"
    />


                        </Grid>


                        <Grid VerticalAlignment="Center" Grid.ColumnSpan="2">

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="7*"/>
                                <ColumnDefinition Width="1*"/>
                            </Grid.ColumnDefinitions>

                            <TextBox x:Name="SearchBox" 
                                     
       Grid.Column="0"
       VerticalAlignment="Center" 
       Margin="0 0 0 0"  
       Height="30"                     
       Style="{StaticResource RoundedTextBoxStyle}"
       HorizontalAlignment="Stretch"
       KeyDown="SearchBox_KeyDown"/>

                            <Button x:Name="SearchBtn"
      Grid.Column="1"
      HorizontalContentAlignment="Center"
      VerticalContentAlignment="Center"                        
      FontSize="20" 
      Content="поиск" 
      Width="100" 
      Height="35"       
      Style="{StaticResource ButtonStyle}"  
      Margin="0 0 0 0"
      Padding="0" Click="SearchBtn_Click"/>
                        </Grid>

                        <ListView   IsEnabled="True"  x:Name="TracksListView" Grid.Row="1" Margin="0,41,0,83" ItemsSource = "{Binding Tracks}"
            ScrollViewer.HorizontalScrollBarVisibility="Disabled" SelectionChanged="TracksListView_SelectionChanged"
               ScrollViewer.VerticalScrollBarVisibility = "Hidden" ScrollViewer.CanContentScroll="False"
             Background="Transparent" Grid.ColumnSpan="2" Grid.RowSpan="3">





                            <ListView.ItemTemplate >
                                <DataTemplate>
                                    <Border BorderBrush="Transparent" BorderThickness="0,0,0,1"  Padding="10">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="auto"/>
                                                <ColumnDefinition Width="auto"/>
                                                <ColumnDefinition Width="auto"/>
                                                <ColumnDefinition Width="2*"/>
                                                <ColumnDefinition Width="auto"/>
                                            </Grid.ColumnDefinitions>
                                            <Image Source="{Binding ImageData}" Width="50" Height="50" Margin="10,0,10,0"/>
                                            <StackPanel Grid.Column="1">
                                                <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="16" HorizontalAlignment="Left"/>
                                                <TextBlock Text="{Binding Artist}" Foreground="#555" FontSize="14"/>
                                            </StackPanel>

                                            <TextBlock Grid.Column="2" 
                                 Text="{Binding Duration}"
                                 Margin="10 18 10 10"
                                 VerticalAlignment="Center"
                                 FontSize="14"/>


                                            <StackPanel Grid.Column="3" HorizontalAlignment="Right" Orientation="Horizontal">
                                                <Button x:Name="add"  Content="+" HorizontalAlignment="Right" 
                                                VerticalAlignment="Stretch" Background="Black" Height="25"  Margin="0 0 0 0" Width="25" Click="add_Click"
                                                            HorizontalContentAlignment="Center"  
                                                VerticalContentAlignment="Center"
                                                Padding="0"         
                                                        />

                                                <Button x:Name="add_ALL" Content="в все треки" Width="100" Height="25" 
                                                       HorizontalAlignment="Right" Margin="10 0 0 0"
                                                        Background="Black" Click="addALL_Click"
                                                        HorizontalContentAlignment="Center"  
                                                        VerticalContentAlignment="Center"
                                                        Padding="0"      
                                                        />
                                                <Button x:Name="add_Play" Content="в плейлист" Width="100" Height="25" 
                                                        Margin="10 0 30 0" 
                                                        HorizontalContentAlignment="Center"  
                                                        VerticalContentAlignment="Center"
                                                    
                                                    Padding="0"         
                                                        HorizontalAlignment="Right" Background="Black" Click="addPlay_Click"/>
                                            </StackPanel>


                                        </Grid>

                                    </Border>
                                </DataTemplate>
                            </ListView.ItemTemplate>

                            <ListView.ItemContainerStyle>
                                <Style TargetType="{x:Type ListViewItem}">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="{x:Type ListViewItem}">
                                                <Border x:Name="Bd" 
                            BorderBrush="Transparent" 
                            BorderThickness="0" 
                            Background="Transparent" 
                            Padding="{TemplateBinding Padding}" 
                            SnapsToDevicePixels="true">
                                                    <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                        SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                                </Border>

                                                <ControlTemplate.Triggers>
                                                    <!-- Полностью отключаем все состояния выделения -->
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" TargetName="Bd" Value="Transparent"/>
                                                    </Trigger>
                                                    <Trigger Property="IsSelected" Value="True">
                                                        <Setter Property="Background" TargetName="Bd" Value="Transparent"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ListView.ItemContainerStyle>
                        </ListView>
                    </Grid>
                </TabItem>
                <TabItem Background="Gray" Header="Плейлисты">


                    <TabControl>
                       
                        <TabItem Header="Все треки">
                            <ListView>
                                
                            </ListView>
                        </TabItem>
                        <TabItem Header="Плейлисты">
                            <TextBlock Text="Содержимое под-вкладки 2" Margin="10"/>
                        </TabItem>
                       
                    </TabControl>




                </TabItem>
            </TabControl>

            <Grid Grid.Row="0" Grid.Column="2" Margin="0 20 20 0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" MinWidth="100"/>

                    <ColumnDefinition Width="Auto" MinWidth="50"/>
                </Grid.ColumnDefinitions>
            </Grid>
        </Grid>
    </Grid>
</Window>