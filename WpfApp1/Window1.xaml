<Window x:Class="WpfApp1.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        WindowState="Normal"
        WindowStyle="None"
        mc:Ignorable="d"
        Title="Window1" Height="900" Width="1920">



    <Window.Resources>



        <Style x:Key="CustomSliderStyle" TargetType="Slider">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Black" />
            <Setter Property="Foreground" Value="Black" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Grid>
                            <Track x:Name="PART_Track" Grid.Row="1">
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Ellipse Width="20" Height="20" Fill="Black" />
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                            <Border x:Name="PART_TrackBackground"
                                Background="Transparent"
                                BorderBrush="Black"
                                BorderThickness="1"
                                Height="4"
                                VerticalAlignment="Center"
                                CornerRadius="2"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>


        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Black" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                       BorderBrush="{TemplateBinding BorderBrush}"
                       BorderThickness="1"
                       CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RoundedTextBoxStyle" TargetType="TextBox">
            <Setter Property="BorderThickness" Value="3" />
            <Setter Property="BorderBrush" Value="Black" />
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="10">
                            <ScrollViewer x:Name="PART_ContentHost" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BlackTabItemStyle" TargetType="{x:Type TabItem}">
            <Setter Property="Background" Value="Black"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="DarkGray"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TabItem}">
                        <Border x:Name="Border" 
                            Background="{TemplateBinding Background}" 
                            BorderBrush="{TemplateBinding BorderBrush}" 
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="5,5,0,0"
                            Margin="2,0">
                            <ContentPresenter x:Name="ContentSite"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="Center"
                                          ContentSource="Header"
                                          Margin="{TemplateBinding Padding}"
                                          RecognizesAccessKey="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#333333"/>
                                <Setter TargetName="Border" Property="BorderThickness" Value="1,1,1,0"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#222222"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

        </Style>


    </Window.Resources>








    <Grid x:Name="GridOfWindow" MouseLeftButtonDown="GridOfWindow_MouseLeftButtonDown">



        <Grid>
            <MediaElement x:Name="BackGroundPlayer" LoadedBehavior="Manual" Stretch="UniformToFill" MediaEnded="BackGroundPlayer_MediaEnded" />

        </Grid>

        <Grid>

            <Grid.RowDefinitions>
                <RowDefinition Height="11*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="3*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>



            <Button x:Name="close" Grid.Column="2" Width="25" Height="25" Background="Black" HorizontalAlignment="Right" VerticalAlignment="Top" Click="close_Click"/>
            <Button x:Name="Minimized" Grid.Column="2" Width="25" Height="25" Background="Black" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0 0 30 0" Click="Minimized_Click"/>


            <TabControl BorderThickness="0" x:Name="tab" Grid.Column="1" Grid.Row="0" Background="Transparent">

                <TabItem  Header="Поиск" Style="{StaticResource BlackTabItemStyle}"  >


                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="19*"/>
                            <ColumnDefinition Width="173*"/>
                        </Grid.ColumnDefinitions>



                        <Grid.RowDefinitions>

                            <RowDefinition Height = "0.7*" />
                            <RowDefinition Height = "0.5*" />
                            <RowDefinition Height = "7*" />
                            <RowDefinition Height = "*" />


                        </Grid.RowDefinitions>



                        <Grid Grid.Row="3" Grid.ColumnSpan="2" >

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="0.7*"/>
                                <ColumnDefinition Width="6*"/>
                                <ColumnDefinition Width="0.5*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>


                            <Button x:Name="PlayPauseBtn" Content="Pouse" Style="{StaticResource ButtonStyle}"
        Height="50" Width="50" Grid.Column="1" 
        Margin="0 0 0 0"  HorizontalAlignment="Left" Click="PlayPauseBtn_Click" RenderTransformOrigin="0.5,0.5"/>


                            <MediaElement x:Name="MediaPlayer"  Visibility="Collapsed" 
              LoadedBehavior="Manual" Volume="{Binding ElementName=slider, Path=Value}"  UnloadedBehavior="Stop"/>


                            <Image x:Name="PlaySong" 
    Width="70"
       Grid.Column="0"
    HorizontalAlignment="Left"
   Margin="30 0 0 0"
    Height="60" />


                            <ProgressBar x:Name="ProgressBar" 
       Background="Black" 
       Foreground="White" 
       HorizontalAlignment="Stretch" 
       VerticalAlignment="Center"
       Margin="10 0 20 10"
       Minimum="0"
       Value="0" 
       Grid.Column="2"
       Height="10"
       MouseDown="ProgressBar_MouseDown"
       MouseMove="ProgressBar_MouseMove"
       MouseUp="ProgressBar_MouseUp"
       IsEnabled="True"/>

                            <TextBlock x:Name="Duration" Text="00:00" Foreground="Black" Grid.Column="3" HorizontalAlignment="Right" Margin="0 30 0 0" FontSize="16"/>

                            <Slider x:Name="slider"
       Style="{StaticResource CustomSliderStyle}"
    Height="30"
    Width="100"
  HorizontalAlignment="Right"
   Margin="0 0 10 0"
        Minimum="0" Maximum="1" Value="{Binding ElementName=MediaPlayer,Path=Volume}"
    Background="Transparent" Foreground="Black" BorderBrush="Black" 
                  Grid.Column="4"
    />


                        </Grid>


                        <Grid VerticalAlignment="Center" Grid.ColumnSpan="2">

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="7*"/>
                                <ColumnDefinition Width="1*"/>
                            </Grid.ColumnDefinitions>

                            <TextBox x:Name="SearchBox" 
                                     
       Grid.Column="0"
       VerticalAlignment="Center" 
       Margin="0 0 0 0"  
       Height="30"                     
       Style="{StaticResource RoundedTextBoxStyle}"
       HorizontalAlignment="Stretch"
       KeyDown="SearchBox_KeyDown"/>

                            <Button x:Name="SearchBtn"
      Grid.Column="1"
      HorizontalContentAlignment="Center"
      VerticalContentAlignment="Center"                        
      FontSize="20" 
      Content="поиск" 
      Width="100" 
      Height="35"       
      Style="{StaticResource ButtonStyle}"  
      Margin="0 0 0 0"
      Padding="0" Click="SearchBtn_Click"/>
                        </Grid>


                        <Popup x:Name="PopupPlaylist" IsOpen="False" Placement="Right"  Height="200" Width="200">
                            <Border Background="Transparent">
                                <Grid>


                                    

                                    <ListView BorderThickness="0" SelectionChanged="PlayListView_SelectionChanged_1" x:Name="PlayListView1" Grid.Row="1" Background="Transparent" ItemsSource="{Binding PlayLists}" Grid.ColumnSpan="2">
                                    <ListView.ItemTemplate>
                                        <DataTemplate>
                                            <Border  BorderBrush="Transparent" BorderThickness="0,0,0,1">
                                                <Grid>

                                                    <Grid.ColumnDefinitions>

                                                        <ColumnDefinition Width="auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="auto"/>
                                                    </Grid.ColumnDefinitions>



                                                    <TextBlock Grid.Column="1" Text="{Binding Name}" FontWeight="Bold" FontSize="16" 
    HorizontalAlignment="Left" VerticalAlignment="Center" Foreground="White" 
    Margin="10,0"/>


                                                    

                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                   

                                </ListView>

    

                                    </Grid>
                            </Border>

                        </Popup>


                        <ListView x:Name="TracksListView" BorderThickness="0" Grid.Row="1" Margin="0,41,0,83" ItemsSource="{Binding Tracks}"
          ScrollViewer.HorizontalScrollBarVisibility="Disabled" SelectionChanged="TracksListView_SelectionChanged"
          ScrollViewer.VerticalScrollBarVisibility="Hidden" ScrollViewer.CanContentScroll="False"
          Background="Transparent" Grid.ColumnSpan="2" Grid.RowSpan="3">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="Transparent" BorderThickness="0,0,0,1" Padding="10">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <!-- изображение сука какие же неудобные комментарии -->
                                                <ColumnDefinition Width="Auto"/>
                                                <!-- автор и название -->
                                                <ColumnDefinition Width="Auto"/>
                                                <!-- дуратион -->
                                                <ColumnDefinition Width="*"/>
                                                <!-- кнокпи эти самые -->
                                            </Grid.ColumnDefinitions>

                                            
                                           


                                            <Image Source="{Binding ImageData}" Width="50" Height="50" Margin="10,0,10,0"/>
                                            <StackPanel Grid.Column="1">
                                                <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="16" HorizontalAlignment="Left"/>
                                                <TextBlock Text="{Binding Artist}" Foreground="#555" FontSize="14"/>
                                            </StackPanel>
                                            <TextBlock Grid.Column="2" Text="{Binding Duration}" Margin="10 18 10 10" VerticalAlignment="Center" FontSize="14"/>
                                            <StackPanel Grid.Column="3" HorizontalAlignment="Right" Orientation="Horizontal">
                                                <Button x:Name="add" Content="+" HorizontalAlignment="Right" VerticalAlignment="Stretch" 
                                Background="Black" Height="25" Width="25" Margin="0 0 0 0" Click="add_Click" Padding="0" Foreground="White" />
                                                <Button x:Name="add_ALL" Content="в все треки" Width="100" Height="25" Foreground="White"
                                HorizontalAlignment="Right" Margin="10 0 0 0" Background="Black" Click="addALL_Click" Padding="0"/>
                                                <Button x:Name="AddPlay" Content="в плейлист" Width="100" Height="25" 
        Margin="10 0 30 0" Background="Black" Foreground="White" 
        Click="AddPlay_Click_1" Padding="0">


                                                    


                                                </Button>

                                            </StackPanel>

      

                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                            <ListView.ItemContainerStyle>
                                <Style TargetType="{x:Type ListViewItem}">
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>




                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="{x:Type ListViewItem}">
                                                <Border x:Name="Bd" BorderBrush="Transparent" BorderThickness="0" Background="Transparent" 
                                Padding="{TemplateBinding Padding}" SnapsToDevicePixels="true">
                                                    <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" TargetName="Bd" Value="Transparent"/>
                                                    </Trigger>
                                                    <Trigger Property="IsSelected" Value="True">
                                                        <Setter Property="Background" TargetName="Bd" Value="Transparent"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ListView.ItemContainerStyle>
                        </ListView>
                    </Grid>
                </TabItem>
                <TabItem Style="{StaticResource BlackTabItemStyle}" Header="Плейлисты">


                    <TabControl BorderThickness="0" Background="Transparent">

                        <TabItem Style="{StaticResource BlackTabItemStyle}" Header="Все треки">
                            <ListView BorderThickness="0" IsEnabled="True" x:Name="TracksListView2" Grid.Row="1" Margin="0,41,0,83" ItemsSource="{Binding ALLTracks}"
          ScrollViewer.HorizontalScrollBarVisibility="Disabled" SelectionChanged="TracksListView_SelectionChanged"
          ScrollViewer.VerticalScrollBarVisibility="Hidden" ScrollViewer.CanContentScroll="False"
          Background="Transparent" Grid.ColumnSpan="2" Grid.RowSpan="3">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <Border BorderBrush="Transparent" BorderThickness="0,0,0,1" Padding="10">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <!-- изображение сука какие же неудобные комментарии -->
                                                    <ColumnDefinition Width="Auto"/>
                                                    <!-- автор и название -->
                                                    <ColumnDefinition Width="Auto"/>
                                                    <!-- дуратион -->
                                                    <ColumnDefinition Width="*"/>
                                                    <!-- кнокпи эти самые -->
                                                </Grid.ColumnDefinitions>


                                                <Image Source="{Binding ImageData}" Width="50" Height="50" Margin="10,0,10,0"/>


                                                <StackPanel Grid.Column="1">
                                                    <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="16" HorizontalAlignment="Left"/>
                                                    <TextBlock Text="{Binding Artist}" Foreground="#555" FontSize="14"/>
                                                </StackPanel>

                                                <TextBlock Grid.Column="2" Text="{Binding Duration}" Margin="10 18 10 10" VerticalAlignment="Center" FontSize="14"/>

                                                <StackPanel Grid.Column="3" HorizontalAlignment="Right" Orientation="Horizontal">
                                                    <Button x:Name="add" Content="+" HorizontalAlignment="Right" VerticalAlignment="Stretch" 
                                Background="Black" Height="25" Width="25" Foreground="White" Margin="0 0 0 0" Click="add_Click" 
                                HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Padding="0"/>
                                                    <Button x:Name="Dell" Content="удалить" Width="100" Height="25" 
                                HorizontalAlignment="Right" Foreground="White" Margin="10 0 0 0" Background="Black" Click="Dell_Click" 
                                HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Padding="0"/>
                                                    <Button x:Name="add_Play" Content="в плейлист" Width="100" Height="25" 
                                Margin="10 0 30 0" HorizontalAlignment="Right" Background="Black" Click="add_Play_Click" 
                                HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Padding="0" Foreground="White"/>
                                                </StackPanel>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                                <ListView.ItemContainerStyle>
                                    <Style TargetType="{x:Type ListViewItem}">
                                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>

                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="{x:Type ListViewItem}">
                                                    <Border x:Name="Bd" BorderBrush="Transparent" BorderThickness="0" Background="Transparent" 
                                Padding="{TemplateBinding Padding}" SnapsToDevicePixels="true">
                                                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" TargetName="Bd" Value="Transparent"/>
                                                        </Trigger>
                                                        <Trigger Property="IsSelected" Value="True">
                                                            <Setter Property="Background" TargetName="Bd" Value="Transparent"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ListView.ItemContainerStyle>
                            </ListView>
                        </TabItem>
                        <TabItem Header="Плейлисты" BorderThickness="0" Style="{StaticResource BlackTabItemStyle}">

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="49.086"/>
                                    <ColumnDefinition/>
                                </Grid.ColumnDefinitions>

                                <Grid.RowDefinitions>
                                    <RowDefinition Height="0.1*"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <Button x:Name="NewPlayList" BorderThickness="0" Content="создать плейлист" Grid.Row="0" Background="Transparent" Opacity="0.9" Foreground="Black"
                                        FontSize="35" Click="NewPlayList_Click" Grid.ColumnSpan="2" Margin="0,0,0,686" Grid.RowSpan="2" >


                                </Button>

                                <Button x:Name="BackToPlaylist" Content="Назад" Grid.Column="1" Background="Transparent" BorderThickness="0" Click="BackToPlaylist_Click"/>


                                <Popup Name="myPopup" IsOpen="False " Placement="Center" Grid.ColumnSpan="2" Margin="0,0,0,686" Grid.RowSpan="2">
                                    <Border BorderThickness="1"  HorizontalAlignment="Center" VerticalAlignment="Center">

                                        <StackPanel>

                                            <TextBox Width="200" Height="50" x:Name="namePlaylistBox" Background="Black" Foreground="White" Text="Новый плейлист"/>

                                            <Button Width="200" Height="50" Content="Создать плейлист" Background="Black" Foreground="White" x:Name="NewPlaylist" Click="NewPlaylist_Click"/>

                                            <Button Width="200" Height="50" Content="Отменить" Background="Black" Foreground="White" x:Name="cancel" Click="cancel_Click" />

                                        </StackPanel>

                                    </Border>
                                </Popup>


                                <ListView x:Name="TracksListView1" Visibility="Collapsed" BorderThickness="0" Grid.Row="1" Margin="0,41,0,83" ItemsSource="{Binding PlayListsTrack}"
  ScrollViewer.HorizontalScrollBarVisibility="Disabled" SelectionChanged="TracksListView_SelectionChanged"
  ScrollViewer.VerticalScrollBarVisibility="Hidden" ScrollViewer.CanContentScroll="False"
  Background="Transparent" Grid.ColumnSpan="2" Grid.RowSpan="3">
                                    <ListView.ItemTemplate>
                                        <DataTemplate>
                                            <Border BorderBrush="Transparent" BorderThickness="0,0,0,1" Padding="10">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <!-- изображение сука какие же неудобные комментарии -->
                                                        <ColumnDefinition Width="Auto"/>
                                                        <!-- автор и название -->
                                                        <ColumnDefinition Width="Auto"/>
                                                        <!-- дуратион -->
                                                        <ColumnDefinition Width="*"/>
                                                        <!-- кнокпи эти самые -->
                                                    </Grid.ColumnDefinitions>





                                                    <Image Source="{Binding ImageData}" Width="50" Height="50" Margin="10,0,10,0"/>
                                                    <StackPanel Grid.Column="1">
                                                        <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="16" HorizontalAlignment="Left"/>
                                                        <TextBlock Text="{Binding Artist}" Foreground="#555" FontSize="14"/>
                                                    </StackPanel>
                                                    <TextBlock Grid.Column="2" Text="{Binding Duration}" Margin="10 18 10 10" VerticalAlignment="Center" FontSize="14"/>
                                                    <StackPanel Grid.Column="3" HorizontalAlignment="Right" Orientation="Horizontal">
                                                        <Button x:Name="add" Content="+" HorizontalAlignment="Right" VerticalAlignment="Stretch" 
                        Background="Black" Height="25" Width="25" Margin="0 0 0 0" Click="add_Click" Padding="0" Foreground="White" />
                                                        <Button x:Name="add_ALL" Content="в все треки" Width="100" Height="25" Foreground="White"
                        HorizontalAlignment="Right" Margin="10 0 0 0" Background="Black" Click="addALL_Click" Padding="0"/>
                                                        <Button x:Name="AddPlay" Content="в плейлист" Width="100" Height="25" 
Margin="10 0 30 0" Background="Black" Foreground="White" 
Click="AddPlay_Click_1" Padding="0">





                                                        </Button>

                                                    </StackPanel>



                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                    <ListView.ItemContainerStyle>
                                        <Style TargetType="{x:Type ListViewItem}">
                                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>




                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="{x:Type ListViewItem}">
                                                        <Border x:Name="Bd" BorderBrush="Transparent" BorderThickness="0" Background="Transparent" 
                        Padding="{TemplateBinding Padding}" SnapsToDevicePixels="true">
                                                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                      SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Background" TargetName="Bd" Value="Transparent"/>
                                                            </Trigger>
                                                            <Trigger Property="IsSelected" Value="True">
                                                                <Setter Property="Background" TargetName="Bd" Value="Transparent"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </ListView.ItemContainerStyle>
                                </ListView>


                                <ListView BorderThickness="0" SelectionChanged="PlayListView_SelectionChanged" x:Name="PlayListView" Grid.Row="1" Background="Transparent" ItemsSource="{Binding PlayLists}" Grid.ColumnSpan="2">
                                    <ListView.ItemTemplate>
                                        <DataTemplate>
                                            <Border  BorderBrush="Transparent" BorderThickness="0,0,0,1">
                                                <Grid>

                                                    <Grid.ColumnDefinitions>

                                                        <ColumnDefinition Width="auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="auto"/>
                                                    </Grid.ColumnDefinitions>

                                                    <Image Grid.Column="0" Source="{Binding image}" Width="50" Height="50" 
                           HorizontalAlignment="Center" VerticalAlignment="Center" Margin="5,0"/>


                                                    <TextBlock Grid.Column="1" Text="{Binding Name}" FontWeight="Bold" FontSize="16" 
                               HorizontalAlignment="Left" VerticalAlignment="Center" Foreground="Black" 
                               Margin="10,0"/>


                                                    <Button Grid.Column="2" x:Name="DelPlayList" Content="Удалить" 
                            Click="DelPlayList_Click" Foreground="White" Background="Black" VerticalAlignment="Center" Height="25" Width="80"
                            HorizontalAlignment="Right" Margin="0,0,10,0"/>

                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                    <!-- хуета что бы кнопки с правого края были -->
                                    <ListView.ItemContainerStyle>
                                        <Style TargetType="ListViewItem">
                                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                            <Setter Property="Background" Value="Transparent"/>
                                            <Setter Property="BorderThickness" Value="0"/>
                                        </Style>
                                    </ListView.ItemContainerStyle>

                                </ListView>
                            </Grid>


                        </TabItem>

                    </TabControl>




                </TabItem>
            </TabControl>

            <Grid Grid.Row="0" Grid.Column="2" Margin="0 20 20 0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" MinWidth="100"/>

                    <ColumnDefinition Width="Auto" MinWidth="50"/>
                </Grid.ColumnDefinitions>
            </Grid>
        </Grid>
    </Grid>
</Window>